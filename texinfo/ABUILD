# ABUILD generated by mkpkg_generator.sh

pkgname=texinfo
pkgver=4.13a
pkgbuild=4
arch=("auto")

shortdesc=("texinfo (GNU software documentation system)")
longdesc=("'Texinfo' is a documentation system that uses a single source file to produce both on-line information and printed output. Using Texinfo, you can create a printed document with the normal features of a book, including chapters, sections, cross references, and indices. From the same Texinfo source file, you can create a menu-driven, on-line Info file with nodes, menus, cross references, and indices. This package is needed to read the documentation files in /usr/info.")

tags=("console app-text")

source=("ftp://ftp.gnu.org/pub/gnu/${pkgname}/${pkgname}-${pkgver}.tar.gz")

build_deps="ncurses findutils gzip"
adddep="findutils gzip ncurses"

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc"
INSTALL_KEYS="DESTDIR=$pkgdir"

after_build() {
	sed -i 's#A-z#A-Za-z#' ${pkgdir}/usr/bin/texi2dvi
}

