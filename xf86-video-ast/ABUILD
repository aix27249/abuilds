# ABUILD generated by mkpkg_generator.sh

pkgname=xf86-video-ast
pkgver=0.97.0
pkgbuild=1
arch=("auto")

shortdesc=("xf86-video-ast: X11 driver for ASPEED AST Graphics cards")
longdesc=("xf86-video-ast is a driver for ASPEED AST Graphics video cards")

tags=("xserver x11-drivers")

source=("http://xorg.freedesktop.org/archive/individual/driver/${pkgname}-${pkgver}.tar.bz2")


build_deps="xorg-server"

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc --disable-static"
INSTALL_KEYS="DESTDIR=$pkgdir"

