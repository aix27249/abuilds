# ABUILD generated by mkpkg_generator.sh

pkgname=dbus-python
pkgver=0.83.2
pkgbuild=2
arch=("noarch")

shortdesc=("Python bindings for dbus (Virtual package)")

tags="virtual dev-python"

build_deps="binutils"

adddep="python-dbus"

build(){
    echo "End build"
}