# ABUILD generated by mkpkg_generator.sh

pkgname=liberation-fonts-ttf
pkgver=2.00.1
pkgbuild=1
arch=("auto")

shortdesc=("Red Hats Liberation fonts.")
longdesc=("Liberation is the collective name of three TrueType font families: Liberation Sans, Liberation Serif and Liberation Mono. These fonts are metric-compatible with Arial, Times New Roman, and Courier New respectively. The fonts were developed by Steve Matteson of Ascender Corp. for Red Hat, Inc.")

tags=("media-fonts xserver")

source=("https://fedorahosted.org/releases/l/i/liberation-fonts/liberation-fonts-ttf-$pkgver.tar.gz")
build_deps="mkpkg"

build() {
  go_src_dir
  install -d "$pkgdir/usr/share/fonts/TTF/"
  install -m644 *.ttf "$pkgdir/usr/share/fonts/TTF/"
  install -Dm0644 $filedir/60-liberation.conf "$pkgdir/etc/fonts/conf.avail/60-liberation.conf"

  # install license
  install -Dm644 LICENSE "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
}