# ABUILD generated by mkpkg_generator.sh

pkgname=hicolor-icon-theme
pkgver=0.13
pkgbuild=1
arch=("noarch")

shortdesc=("Freedesktop.org Hicolor icon theme (default)")

longdesc=("This is the default fallback theme used by implementations of the icon theme specification. The specification is available at: http://www.freedesktop.org/standards/icon-theme-spec/")

tags=("libs x11-themes")

source=("http://icon-theme.freedesktop.org/releases/${pkgname}-${pkgver}.tar.gz")

build_deps="bash coreutils"

BUILD_SYSTEM="autotools"

BUILD_KEYS="--prefix=/usr \
--libdir=/usr/lib$LIBDIRSUFFIX \
--mandir=/usr/man \
--sysconfdir=/etc"

INSTALL_KEYS="DESTDIR=${pkgdir}"
