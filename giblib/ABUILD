# ABUILD generated by mkpkg_generator.sh

pkgname=giblib
pkgver=1.2.4
pkgbuild=3
arch=("auto")

shortdesc=("Wrapper library for imlib2, and other stuff")
longdesc=("giblib is a library of handy stuff. Contains an imlib2 wrapper to avoid the context stuff, doubly-linked lists and font styles.")
tags=("libs media-libs")
source=("http://linuxbrit.co.uk/downloads/${pkgname}-${pkgver}.tar.gz")
build_deps="glibc-solibs freetype imlib2 libpthread-stubs libX11 libXau libxcb libXdmcp libXext"


BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc"
INSTALL_KEYS="DESTDIR=$pkgdir docsdir=/usr/doc/giblib"

