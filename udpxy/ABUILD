# ABUILD generated by mkpkg_generator.sh

pkgname=udpxy
pkgver=1.0
pkgbuild=1
arch=("auto")

shortdesc=("Small-footprint UNIX/Linux daemon to relay multicast UDP traffic to client's TCP (HTTP) connection")
longdesc=("")

tags=("net-misc network")

source=("http://citylan.dl.sourceforge.net/project/udpxy/udpxy/Chipmunk-${pkgver}/udpxy.${pkgver}-Chipmunk-PATCH120.tgz")

build_deps=""


build() {
	go_src_dir
	burn_patches
	make -j${numjobs}
	install -Dm0755 udpxy ${pkgdir}/usr/bin/udpxy
}
