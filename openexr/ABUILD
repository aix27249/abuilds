# ABUILD generated by mkpkg_generator.sh

pkgname=openexr
pkgver=2.0.0
pkgbuild=1
arch=("auto")

shortdesc=("HDR Image File Format & Library")
longdesc=("OpenEXR is an image file format and library developed by Industrial Light & Magic, and later released to the public. It provides support for high dynamic range and a 16-bit floating point "half" data type which is compatible with the half data type in the Cg programming language. Homepage: http://www.openexr.org/")

tags=("libs media-libs")

source=("http://download.savannah.nongnu.org/releases/${pkgname}/${pkgname}-${pkgver}.tar.gz")
build_deps="cxxlibs glibc-solibs ilmbase"

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc"
INSTALL_KEYS="DESTDIR=$pkgdir"

after_build() {
  mv ${pkgdir}/usr/{share/doc/OpenEXR-$pkgver/*,doc/$pkgname-$pkgver}
  rm -r ${pkgdir}/usr/share/doc
}