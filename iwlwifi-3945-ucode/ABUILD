# ABUILD generated by mkpkg_generator.sh

pkgname=iwlwifi-3945-ucode
pkgver=15.32.2.9
pkgbuild=2
arch=("fw")

shortdesc=("iwlwifi-3945-ucode (Intel 3945 wireless microcode)")
longdesc=("The microcode provided in this package is required for the Intel PRO/Wireless 3945ABG/BG Network Connection Adapter driver for Linux (iwlwifi-3945) to operate. Visit Intel's Linux wireless website at this address: http://intellinuxwireless.org/")

tags=("net-wireless network")

source=("http://intellinuxwireless.org/iwlwifi/downloads/iwlwifi-3945-ucode-${pkgver}.tgz")

build() {
	go_src_dir
	mkdir -p ${pkgdir}/lib/firmware/
	cp iwlwifi-*.ucode ${pkgdir}/lib/firmware/
}
