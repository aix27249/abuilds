# ABUILD generated by spkg2abuild converter from gst-plugins-good-0.10.21-1.spkg

# Package metadata
pkgname=gst-plugins-ugly
pkgver=0.10.18
pkgbuild=2
arch="auto"

# Package description
shortdesc=("GStreamer plugins - ugly ones")
longdesc=("GStreamer plugins - ugly ones")

# Source URL
source=("http://gstreamer.freedesktop.org/src/gst-plugins-ugly/gst-plugins-ugly-${pkgver}.tar.bz2")

# Tags
tags="libs media-libs"

before_build() {
	go_src_dir
#	cp ${filedir}/orc.m4 common/m4
}

# Build system
BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr \
--mandir=/usr/man \
--sysconfdir=/etc \
--localstatedir=/var \
--libdir=/usr/lib${LIBDIRSUFFIX} \
--enable-experimental \
--disable-static \
--with-package-name='GStreamer Ugly Plugins (AgiliaLinux)' \
	--with-package-origin='http://www.agilialinux.ru/'"
BUILD_WORDS="make -j${numjobs} && sed -e 's/gst sys ext/gst/' -i Makefile"
INSTALL_KEYS="DESTDIR=${pkgdir}"
