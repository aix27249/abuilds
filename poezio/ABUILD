# ABUILD generated by mkpkg_generator.sh

pkgname=poezio
pkgver=0.7.2
pkgbuild=1
arch=("auto")

shortdesc=("Console XMPP client")

tags=("net-im console")

source=("http://dev.louiz.org/project/poezio/download/file/poezio-${pkgver}.tar.xz")

build_deps="python-three"
adddep="sleekxmpp dnspython3"

build() {
	go_src_dir
	make prefix=/usr -j${numjobs}
	make install prefix=/usr DESTDIR=${pkgdir}
}
