pkgname=ConsoleKit
pkgver=0.4.5
pkgbuild=4
arch=('noarch')

shortdesc="A framework for defining and tracking users, login sessions, and seats (Virtual package)"

tags="virtual sys-auth"

build_deps="bash"

adddep="consolekit"

build(){
  echo "End build"
}

