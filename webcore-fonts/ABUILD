# ABUILD generated by mkpkg_generator.sh

pkgname=webcore-fonts
pkgver=3.0
pkgbuild=2
arch=("noarch")

shortdesc=("Core fonts for the Web")
longdesc=("")

tags=("media-fonts xserver")

source=("http://avi.alkalay.net/software/webcore-fonts/webcore-fonts-${pkgver}.tar.gz")

build() {
	go_src_dir
	set -e
	mkdir -p ${pkgdir}/usr/share/fonts/TTF
	cp -a fonts/* ${pkgdir}/usr/share/fonts/TTF
	cp -a vista/* ${pkgdir}/usr/share/fonts/TTF
	set +e

}
