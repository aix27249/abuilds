# ABUILD generated by mkpkg_generator.sh

pkgname=laptop-detect
pkgver=0.13.7
pkgbuild=2
arch=("noarch")

shortdesc=("attempt to detect a laptop")
longdesc=("laptop-detect attempts to determine whether it is being run on a laptop or a desktop and appraises its caller of this. Returns 0 if laptop, 1 otherwise.")

tags=("utils app-admin")

build_deps="mkpkg"
adddep="dmidecode"

source=("http://ftp.de.debian.org/debian/pool/main/l/laptop-detect/laptop-detect_${pkgver}.tar.gz")

build() {
	go_src_dir
	install -D -m0755 laptop-detect.in ${pkgdir}/usr/sbin/laptop-detect
	install -D -m0644 laptop-detect.8 ${pkgdir}/usr/man/man8/laptop-detect.8
}
