# ABUILD generated by mkpkg_generator.sh

pkgname=wimax-network-service
pkgver=1.5.2
pkgbuild=1
arch=("auto")

shortdesc=("WiMAX networking servie")

tags=("network net-wireless")

source=("ftp://ftp.be.debian.org/pub/linux/kernel/people/inaky/wimax-${pkgver}.tar.bz2")

build_deps="make gcc coreutils pkg-config binutils sed grep kernel-headers wimax-tools dhclient libeap"

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr \
            --libdir=/usr/lib${LIBDIRSUFFIX} \
            --sysconfdir=/etc \
            --localstatedir=/var \
            --mandir=/usr/man"

INSTALL_KEYS="DESTDIR=${pkgdir}"


