# ABUILD generated by mkpkg_generator.sh

pkgname=glibc
pkgver=2.12.2
pkgbuild=1
arch=("noarch")

shortdesc=("glibc (GNU C libraries) - virtual package")
longdesc=("Virtual package that collects all glibc components.")

tags=("virtual sys-libs")

addep="glibc-solibs==${pkgver} glibc-i18n==${pkgver}"

build() {
	echo "Building virtual package"
}
