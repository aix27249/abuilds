# ABUILD generated by spkg2abuild converter from gtkmm-2.18.2-2.spkg

# Package metadata
pkgname=gtkmm
pkgver=2.24.2
pkgbuild=1
arch="auto"

# Package description
shortdesc=("C++ interface for GTK+2")
longdesc=("gtkmm is the official C++ interface for the popular GUI library GTK+. Highlights include typesafe callbacks, and a comprehensive set of widgets that are easily extensible via inheritance.")

# Source URL
source=("http://ftp.gnome.org/pub/GNOME/sources/gtkmm/2.24/gtkmm-${pkgver}.tar.bz2")

# Tags
tags="libs dev-cpp"

# Automatically-generated build_deps. Subject to revision.
build_deps="atk atkmm bzip2 cairo cairomm gcc-libs expat fontconfig freetype gcc gdk-pixbuf glib2 glibc-solibs glibmm gtk+2 libpng libsigc++ libX11 libXau libxcb libXdmcp libXrender pango pangomm pixman zlib"

# Build system
BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr \
--libdir=/usr/lib${LIBDIRSUFFIX} \
--mandir=/usr/man \
--sysconfdir=/etc \
--localstatedir=/var"


