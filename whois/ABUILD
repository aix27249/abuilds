# ABUILD generated by mkpkg_generator.sh

pkgname=whois
pkgver=5.0.11
pkgbuild=1
arch=("auto")

shortdesc=("whois (whois directory client)")
longdesc=("This is an enhanced whois (RFC 954) client derived from the BSD and RIPE whois programs. It can automatically select the appropriate whois server for most queries. This version of whois was written by Marco d'Itri For more information, see: http://www.linux.it/~md/software/")

tags=("network net-misc")

source=("http://ftp.debian.org/debian/pool/main/w/whois/${pkgname}_${pkgver}.tar.gz")

build() {
	go_src_dir
	burn_patches
	make prefix=/usr HAVE_LIBIDN=y
	make install install-pos BASEDIR=${pkgdir}
}
