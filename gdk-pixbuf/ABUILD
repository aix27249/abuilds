pkgname=gdk-pixbuf
pkgver=2.26.1
pkgbuild=3
arch=('auto')

shortdesc="GDK Pixbuf library"
longdesc=("An image loading library for gtk2/gtk3"
)

source=("http://download.gnome.org/sources/$pkgname/${pkgver%.*}/$pkgname-${pkgver}.tar.xz")

tags="libs x11-libs"

# Automatically-generated build_deps. Subject to revision.
build_deps="glibc-solibs libpng libxcb libXdmcp libXau libX11 libffi xz zlib libjpeg glib2 libtiff"



BUILD_SYSTEM="autotools"

BUILD_WORD="LDFLAGS=\"$SLKLDFLAGS\" CFLAGS=\"$SLKCFLAGS\" CXXFLAGS=\"$SLKCFLAGS\" ./configure"

BUILD_KEYS="--prefix=/usr \
--sysconfdir=/etc \
--localstatedir=/var \
--libdir=/usr/lib${LIBDIRSUFFIX} \
--disable-static \
--mandir=/usr/man \
--program-prefix= \
--program-suffix= \
--without-libjasper \
--with-x11 \
--with-included-loaders=png"

INSTALL_KEYS="DESTDIR=$pkgdir"

after_build() {
	# Compatibility stuff
	mkdir $pkgdir/usr/include/gtk-2.0
	ln -s /usr/include/gdk-pixbuf-2.0/* $pkgdir/usr/include/gtk-2.0
}
