# ABUILD generated by mkpkg_generator.sh

pkgname=ilmbase
pkgver=2.0.1
pkgbuild=1
arch=("auto")

shortdesc=("Industrial Light & Magic base libraries")

longdesc=("ilmbase currently includes: IlmThread - a thread abstraction library Imath - a math function library Iex - an excpetion-handling library Half - Ilm's 16 bit floating point format library Homepage: http://www.openexr.org/")

tags=("libs media-libs")

source=("http://download.savannah.nongnu.org/releases/openexr/${pkgname}-${pkgver}.tar.gz")

build_deps="make gcc gcc-g++ binutils sed grep coreutils cxxlibs glibc-solibs"

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr \
            --libdir=/usr/lib$LIBDIRSUFFIX \
            --mandir=/usr/man \
            --sysconfdir=/etc"

INSTALL_KEYS="DESTDIR=${pkgdir}"
