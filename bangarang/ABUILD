# ABUILD generated by mkpkg_generator.sh
pkgname=bangarang
pkgver=2.2
pkgbuild=1
arch=("auto")

shortdesc=("Bangarang is a free media player for KDE4, built using Nepomuk to provide a semantic view of your music.")

tags=("kde4 media-sound")

source="git:git://anongit.kde.org/$pkgname"

BUILD_SYSTEM="cmake"
BUILD_KEYS="-DCMAKE_INSTALL_PREFIX=/usr \
	-DLIB_SUFFIX=${LIBDIRSUFFIX} \
	-DCMAKE_BUILD_TYPE=Release"
INSTALL_KEYS="DESTDIR=$pkgdir"

