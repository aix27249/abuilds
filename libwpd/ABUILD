# ABUILD generated by mkpkg_generator.sh

pkgname=libwpd
pkgver=0.10.0
pkgbuild=1
arch=("auto")

shortdesc=("WordPerfect document format library")
longdesc=("WordPerfect document format library")

tags=("libs app-text")

build_deps="gcc"
source=("http://downloads.sourceforge.net/sourceforge/${pkgname}/${pkgname}-${pkgver}.tar.bz2")

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib${LIBDIRSUFFIX} --enable-static"
INSTALL_KEYS="DESTDIR=$pkgdir"

