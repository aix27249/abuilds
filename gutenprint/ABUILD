# ABUILD generated by mkpkg_generator.sh

pkgname=gutenprint
pkgver=5.2.10
pkgbuild=1
arch=("auto")

shortdesc=("gutenprint (IJS printer driver for Ghostscript and CUPS)")
longdesc=("gutenprint (IJS printer driver for Ghostscript and CUPS)")

tags=("console net-print")

source=("http://downloads.sourceforge.net/gimp-print/$pkgname-$pkgver.tar.bz2")

adddep="foomatic-db-engine"

build_deps="make gcc docbook-utils coreutils pkg-config gettext binutils sed grep"

gendeps_blacklist="/usr/lib${LIBDIRSUFFIX}/gimp/"

before_build(){
  go_src_dir
  sed -i "s:m4local:m4extra:" Makefile.am
}

BUILD_SYSTEM="autotools"

BUILD_KEYS="--prefix=/usr \
  --libdir=/usr/lib${LIBDIRSUFFIX} \
  --infodir=/usr/info \
  --mandir=/usr/man \
  --enable-samples \
  --enable-cups-ppds \
  --enable-cups-ppds-at-top-level \
  --disable-translated-cups-ppds \
  --disable-globalized-cups-ppds \
  --disable-static-genppd \
  --with-cups \
  --with-gimp2 \
  --with-gimp2-as-gutenprint \
  --enable-libgutenprintui2 \
  --enable-escputil \
  --disable-static"

INSTALL_KEYS="DESTDIR=${pkgdir}"

