# ABUILD generated by mkpkg_generator.sh

pkgname=ed
pkgver=1.6
pkgbuild=1
arch=("auto")

shortdesc=("ed (text editor)")
longdesc=("GNU ed is an 8-bit clean, more or less POSIX-compliant implementation of the standard Unix line editor. These days, full-screen editors have rendered 'ed' mostly of historical interest. Nonetheless, it appeals to a handful of aging programmers who still believe that "Small is Beautiful".")

tags=("base sys-base")

source=("ftp://ftp.gnu.org/gnu/ed/${pkgname}-${pkgver}.tar.gz")
build_deps="glibc-solibs"

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc CFLAGS=\"$CFLAGS\" LDFLAGS=\"$LDFLAGS\""
INSTALL_KEYS="DESTDIR=$pkgdir"

after_build() {
	rm -rf ${pkgdir}/usr/info
}

