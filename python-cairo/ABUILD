# ABUILD generated by mkpkg_generator.sh

pkgname=python-cairo
pkgver=1.10.0
pkgbuild=1
arch=("auto")

shortdesc=("pycairo (a set of Python bindings for the cairo graphics library)")
longdesc=("Pycairo is set of Python bindings for the cairo graphics library. Pycairo's web site: http://cairographics.org/pycairo")

tags=("libs dev-python")

source=("http://cairographics.org/releases/py2cairo-${pkgver}.tar.bz2")
build_deps="python cairo pkg-config"
adddep="python cairo"

BUILD_SYSTEM="waf"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib${LIBDIRSUFFIX}"
INSTALL_KEYS="--destdir=$pkgdir"

