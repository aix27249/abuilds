# ABUILD generated by mkpkg_generator.sh

pkgname=cxxlibs
pkgver=6.0.17
pkgbuild=1
arch=("noarch")

shortdesc=("C++ shared libraries (now part of gcc-libs)")

tags=("sys-libs virtual")
build_deps="gcc-g++"
adddep="gcc-libs"
build() {
	echo "Building virtual package"
}
