# ABUILD generated by mkpkg_generator.sh

pkgname=aaa_elflibs-dummy
pkgver=14.0
pkgbuild=6
arch=('noarch')
provides="aaa_elflibs"
shortdesc=('aaa_elflibs-dummy package')
longdesc=('This is a transition package that will smoothly end the legacy of slackware-style package system.')

tags=('sys-base virtual')
adddep="acl attr bzip2 libcap dmapi fuse gpm ncurses sysfsutils libtermcap alsa-lib curl expat freetype gdbm glib2 libidn libjpeg openldap mm gmp pcre libpng popt readline slang1 slang libtiff svgalib zlib libusb lvm2 gcc db42 db44"
build() {
	echo "Oops, it's ready"
}
