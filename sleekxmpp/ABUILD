# ABUILD generated by mkpkg_generator.sh

pkgname=sleekxmpp
pkgver=1.0rc3
srcver=`echo $pkgver | sed 's/rc/-RC/g'`
pkgbuild=1
arch=("auto")

shortdesc=("Python 3.1+ XMPP Library")

tags=("dev-python libs")

source=("https://github.com/fritzy/SleekXMPP/tarball/$srcver")

build_deps="python-three"
removedep="python"
adddep="python-three>=3.2"
build() {
	go_src_dir
	burn_patches
	python3 setup.py build
	python3 setup.py install --root=${pkgdir}
}
