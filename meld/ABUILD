# ABUILD generated by mkpkg_generator.sh

pkgname=meld
pkgver=1.7.5
pkgbuild=1
arch=("auto")

shortdesc=("Visual diff and merge tool")
tags=("xapps dev-util")

source=("http://ftp.gnome.org/pub/gnome/sources/$pkgname/${pkgver%.*}/$pkgname-$pkgver.tar.xz")
build_deps="pygtk intltool desktop-file-utils hicolor-icon-theme rarian"
adddep="python pygtk"

build() {
  go_src_dir
  sed -i 's/update-/#update-/' Makefile
  make -j${numjobs} prefix=/usr
  make prefix=/usr DESTDIR="$pkgdir" install
}
