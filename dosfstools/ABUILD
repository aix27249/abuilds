# ABUILD generated by mkpkg_generator.sh

pkgname=dosfstools
pkgver=3.0.11
pkgbuild=1
arch=("auto")

shortdesc=("dosfstools (tools for working with FAT filesystems)")
longdesc=("Utilities for creating FAT filesystems (mkdosfs), and for checking and repairing them (dosfsck).")

tags=("base sys-fs")

source=("http://www.daniel-baumann.ch/software/dosfstools/$pkgname-$pkgver.tar.bz2")

build() {
	go_src_dir
	burn_patches
	set -e
	make -j${numjobs}
	make PREFIX=$pkgdir MANDIR=$pkgdir/usr/man DOCDIR=$pkgdir/usr/doc install
	set +e
}

