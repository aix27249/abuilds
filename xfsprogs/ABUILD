# ABUILD generated by mkpkg_generator.sh

pkgname=xfsprogs
pkgver=3.1.11
pkgbuild=1
arch=("auto")

shortdesc=("Tools to use the XFS filesystem")
tags=("base sys-fs")

# Use archlinux mirror, since upstream FTP is low and slow
source=("ftp://ftp.archlinux.org/other/xfsprogs/${pkgname}-${pkgver}.tar.gz")

build_deps="util-linux-ng"


BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX \
	--sysconfdir=/etc \
	--libexecdir=/usr/lib${LIBDIRSUFFIX} \
	--sbindir=/sbin \
	--bindir=/usr/sbin \
	--datadir=/usr/share \
	--mandir=/usr/man \
	--enable-shared=yes \
	--enable-gettext=yes"
INSTALL_KEYS="DIST_ROOT=$pkgdir"

after_build() {
	set -e
	make install-dev DIST_ROOT=${pkgdir}
	make install-qa DIST_ROOT=${pkgdir}
	set +e
}
