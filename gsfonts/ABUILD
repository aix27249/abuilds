# ABUILD generated by mkpkg_generator.sh

pkgname=gsfonts
pkgver=1.0.7pre44
_realname=urw-fonts
_fedoraver=2.4
_fedorarel=16.fc20
pkgbuild=1
arch=("auto")

shortdesc=("Standard Ghostscript Type1 fonts from URW")
longdesc=("These fonts are used by the GhostScript interpreter.")
tags=("console media-fonts")
source=("http://kojipkgs.fedoraproject.org/packages/${_realname}/${_fedoraver}/${_fedorarel}/src/${_realname}-${_fedoraver}-${_fedorarel}.src.rpm")
build_deps=" fontconfig rpm libarchive" #xorg-font-utils
conflicts="ghostscript-fonts-std"

build() {
    cd "${srcdir}"
    bsdtar xf *.tar.bz2
    install -d "${pkgdir}"/usr/share/fonts/Type1
    install -m644 *.{pfb,pfm,afm} "${pkgdir}"/usr/share/fonts/Type1
}
