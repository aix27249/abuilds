# ABUILD generated by mkpkg_generator.sh

pkgname=libid3tag
pkgver=0.15.1b
pkgbuild=4
arch=("auto")

shortdesc=("libid3tag (ID3 tag manipulation library)")
longdesc=("libid3tag is a library for reading and (eventually) writing ID3 tags, both ID3v1 and the various versions of ID3v2. libid3tag was written by Robert Leslie.")

tags=("libs media-libs")
source=("ftp://ftp.mars.org/pub/mpeg/${pkgname}-${pkgver}.tar.gz")

build_deps="glibc-solibs gperf"

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc"
INSTALL_KEYS="DESTDIR=$pkgdir"

after_build() {
  install -D -m644 "${filedir}/id3tag.pc" "${pkgdir}/usr/lib/pkgconfig/id3tag.pc"
}