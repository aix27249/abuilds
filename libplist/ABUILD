# ABUILD generated by mkpkg_generator.sh

pkgname=libplist
pkgver=1.8
pkgbuild=1
arch=("auto")

shortdesc=("A library to handle Apple Property List format whereas it's binary or XML")

tags=("libs media-libs")

source=("http://www.libimobiledevice.org/downloads/$pkgname-$pkgver.tar.bz2")

build_deps="cmake swig"


BUILD_SYSTEM="cmake"
BUILD_KEYS="-DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_INSTALL_LIBDIR=/usr/lib${LIBDIRSUFFIX} -DCMAKE_BUILD_TYPE=Release -DCMAKE_SKIP_RPATH=ON"
INSTALL_KEYS="DESTDIR=$pkgdir"

