# ABUILD generated by mkpkg_generator.sh

pkgname=libical
pkgver=0.46
pkgbuild=2
arch=('auto')

shortdesc=('libical (iCAL protocol implementation)')
longdesc=("libical is an Open Source (MPL/LGPL) implementation of the IETF's iCalendar Calendaring and Scheduling protocols. (RFC 2445, 2446, and 2447). It parses iCal components and provides C/C++/Python/Java APIs for manipulating the component properties, parameters, and subcomponents. Homepage: http://freeassociation.sourceforge.net/")

tags=('libs dev-libs')

source=("http://downloads.sourceforge.net/freeassociation/${pkgname}-${pkgver}.tar.gz")

build_deps='aaa_elflibs cxxlibs glibc-solibs'
before_build() {
	go_src_dir
	./autogen.sh
	libtoolize --force
}

BUILD_SYSTEM='autotools'
BUILD_KEYS="--prefix=/usr \
    --enable-shared \
    --libdir=/usr/lib$LIBDIRSUFFIX \
    --disable-static"
INSTALL_KEYS="DESTDIR=$pkgdir"

