# ABUILD generated by spkg2abuild converter from gst-plugins-good-0.10.21-1.spkg

# Package metadata
pkgname=gst-plugins-good
pkgver=0.10.30
pkgbuild=1
arch="auto"

# Package description
shortdesc=("GStreamer plugins - good ones")
longdesc=("GStreamer plugins - good ones")

# Source URL
source=("http://gstreamer.freedesktop.org/src/gst-plugins-good/gst-plugins-good-${pkgver}.tar.bz2")

# Tags
tags="libs media-libs"

# Automatically-generated build_deps. Subject to revision.
build_deps="bzip2 gcc-libs glibc-solibs aalib libjpeg libpng libraw1394 slang1 wavpack orc esound libiec61883 speex libdv gcc GConf libsoup gpm libxml2 libXv libXdamage libXfixes libXext libSM libICE cairo taglib libavc1394 libv4l libX11 flac jack1 glib2 gdk-pixbuf libcaca zlib udev gstreamer gst-plugins-base"


# Build system
BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr \
--mandir=/usr/man \
--sysconfdir=/etc \
--localstatedir=/var \
--libdir=/usr/lib${LIBDIRSUFFIX} \
--disable-schemas-install \
--enable-experimental \
--disable-static \
--without-hal \
--with-package-name='GStreamer Good Plugins (AgiliaLinux)' \
	--with-package-origin='http://www.agilialinux.ru/'"
BUILD_WORDS="make -j${numjobs} && sed -e 's/gst sys ext/gst/' -i Makefile"
INSTALL_KEYS="DESTDIR=${pkgdir}"
