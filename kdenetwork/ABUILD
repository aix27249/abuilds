# ABUILD generated by mkpkg_generator.sh

pkgname=kdenetwork
pkgver=4.10.3
strict_version='yes'
pkgbuild=1
arch=('auto')

shortdesc=('kdenetwork (network utilities for KDE)')
longdesc=('Network related utilities for the K Desktop Environment.')

tags=('kde-base kde4')

#source=("svn:svn://anonsvn.kde.org/home/kde/tags/KDE/${pkgver}/${pkgname}")
source=("ftp://ftp.kde.org/pub/kde/stable/${pkgver}/src/${pkgname}-${pkgver}.tar.xz")


if [ -d /usr/lib${LIBDIRSUFFIX}/qt ]; then
  QTDIR=/usr/lib${LIBDIRSUFFIX}/qt
fi

before_build() {
 echo ""
}
BUILD_SYSTEM='cmake'
BUILD_KEYS="-DCMAKE_BUILD_TYPE=Release \
    -DCMAKE_INSTALL_PREFIX=/usr \
    -DMAN_INSTALL_DIR=/usr/man \
    -DSYSCONF_INSTALL_DIR=/etc \
    -DLIB_SUFFIX=${LIBDIRSUFFIX} \
    -DCMAKE_SKIP_RPATH=ON"
INSTALL_KEYS="DESTDIR=$pkgdir"

after_build() {
 echo ""
}

