# ABUILD generated by mkpkg_generator.sh

pkgname=libimobiledevice
pkgver=1.1.5
pkgbuild=1
arch=("auto")

shortdesc=("A software library that talks the protocols to support iPhone and iPod Touch devices on Linux")

tags=("libs media-libs")

build_deps="make gcc grep binutils gcc-g++ usbmuxd"

adddep="usbmuxd gnutls libgcrypt"

source=("http://libimobiledevice.org/downloads/$pkgname-$pkgver.tar.bz2")

BUILD_SYSTEM="autotools"

BUILD_KEYS="--prefix=/usr \
            --libdir=/usr/lib${LIBDIRSUFFIX} \
            --mandir=/usr/man \
            --docdir=/usr/doc"

