# ABUILD generated by mkpkg_generator.sh

pkgname=peyote
pkgver=0.9.12
_srcver=`echo $pkgver | sed "s/\.1$/-1/g"`
pkgbuild=1
arch=("auto")

shortdesc=("Audio player with mc-like interface written in python.")
longdesc=("Peyote is an audio player with friendly MC-like interface.
Peyote is designed specifically for work easy with cue sheets.
Peyote supports wv ( wavepack ), wav, flac, ape, ogg and mp3 formats.")

tags=("media-sound xapps")

source=("http://sourceforge.net/projects/peyote/files/Linux/peyote_${_srcver}.tar.bz2")

adddep="mutagen gst-python pygobject dbus-python gst-plugins-bad gst-plugins-good gst-plugins-base gst-plugins-ugly gst-ffmpeg pyinotify rxvt-unicode"

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib${LIBDIRSUFFIX}"
INSTALL_KEYS="DESTDIR=$pkgdir"

