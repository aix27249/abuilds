# ABUILD generated by mkpkg_generator.sh

pkgname=iwlwifi-1000-ucode
pkgver=128.50.3.1
pkgbuild=1
arch=("fw")

shortdesc=("iwlwifi-1000-ucode (Intel 1000 wireless microcode)")
longdesc=("The microcode provided in this package is required for the Intel PRO/Wireless 1000ABG/BG Network Connection Adapter driver for Linux (iwlwifi-1000) to operate. Visit Intel's Linux wireless website at this address: http://intellinuxwireless.org/")

tags=("net-wireless network")

source=("http://intellinuxwireless.org/iwlwifi/downloads/iwlwifi-1000-ucode-${pkgver}.tgz")

build() {
	go_src_dir
	mkdir -p ${pkgdir}/lib/firmware/
	cp iwlwifi-*.ucode ${pkgdir}/lib/firmware/
}
