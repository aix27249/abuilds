# ABUILD generated by mkpkg_generator.sh

pkgname=iwlwifi-6000-ucode
pkgver=9.221.4.1
pkgbuild=1
arch=("fw")

shortdesc=("iwlwifi-6000-ucode (Intel 6000 wireless microcode)")
longdesc=("The microcode provided in this package is required for the Intel PRO/Wireless 6000ABG/BG Network Connection Adapter driver for Linux (iwlwifi-6000) to operate. Visit Intel's Linux wireless website at this address: http://intellinuxwireless.org/")

tags=("net-wireless network")

source=("http://intellinuxwireless.org/iwlwifi/downloads/iwlwifi-6000-ucode-${pkgver}.tgz")

build() {
	go_src_dir
	mkdir -p ${pkgdir}/lib/firmware/
	cp iwlwifi-*.ucode ${pkgdir}/lib/firmware/
}
