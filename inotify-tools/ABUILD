# ABUILD generated by mkpkg_generator.sh

pkgname=inotify-tools
pkgver=3.14
pkgbuild=1
arch=("auto")

shortdesc=("inotify-tools (command line utilities for inotify)")
longdesc=("inotify-tools is a set of command-line programs for Linux providing a simple interface to inotify. These programs can be used to monitor and act upon filesystem events. For more information see the inotifywait and inotifywatch man pages. inotify-tools home: http://inotify-tools.sourceforge.net/")

tags=("base sys-fs")

source=("http://github.com/downloads/rvoicilas/inotify-tools/inotify-tools-$pkgver.tar.gz")
build_deps="glibc-solibs make gcc"


BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --docdir=/usr/doc/${pkgname}-${pkgver} --sysconfdir=/etc"
INSTALL_KEYS="DESTDIR=$pkgdir"
