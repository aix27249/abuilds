# ABUILD generated by mkpkg_generator.sh

pkgname=pdftk
pkgver=1.44
pkgbuild=1
arch=("auto")

shortdesc=("Pdftk allows you to manipulate PDF easily and freely.")

tags=("app-text xapps")

source=("http://www.pdflabs.com/tools/pdftk-the-pdf-toolkit/${pkgname}-${pkgver}-src.zip")

build_deps="gcc-gcj-ecj gcc-java"

build() {
	go_src_dir
	burn_patches
	set -e
	( cd pdftk
		# Note: always use single-threaded make, otherwise build fails.
		make -f Makefile.Slackware-13.1
		install -Dm0755 pdftk ${pkgdir}/usr/bin/pdftk
	)
	install -Dm0644 pdftk.1 ${pkgdir}/usr/man/man1/pdftk.1
	set +e
}
