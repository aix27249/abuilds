#ABUILD created by/создан: khvalera, khvalera at narod.ru

pkgname=GConf
pkgver=2.32.0
pkgbuild=3
arch=('noarch')

shortdesc="An old configuration database system for GNOME-based packages."

tags="virtual gnome-base"

build_deps="bash"

adddep="gconf"

build(){
  echo "End build"
}