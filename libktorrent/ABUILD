# ABUILD generated by mkpkg_generator.sh

pkgname=libktorrent
pkgver=1.3.1
_branchver=`echo ${pkgver} | sed s/^1/4/g`
pkgbuild=1
arch=("auto")

shortdesc=("KTorrent runtime library")
longdesc=("KTorrent library, now used in different projects, not only in KTorrent")

tags=("kde4 kde-base")

source=("http://ktorrent.org/downloads/${_branchver}/libktorrent-${pkgver}.tar.bz2")

BUILD_SYSTEM="cmake"
BUILD_KEYS="-DCMAKE_INSTALL_PREFIX=/usr -DLIB_SUFFIX=${LIBDIRSUFFIX} -DCMAKE_BUILD_TYPE=Release"
INSTALL_KEYS="DESTDIR=$pkgdir"


