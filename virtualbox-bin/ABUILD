# ABUILD generated by mkpkg_generator.sh

pkgname=virtualbox-bin
pkgver=4.2.16
pkgbuild=1
arch=("noarch")

shortdesc=("Transitional package for virtualbox (virtual)")
longdesc=("This package makes smooth transition from virtualbox-bin to virtualbox and is virtual")

tags=("app-emulation virtual")

adddep="virtualbox>=$pkgver"

build() {
	echo "Building virtual package..."
}

