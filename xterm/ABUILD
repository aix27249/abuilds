# ABUILD generated by mkpkg_generator.sh

pkgname=xterm
pkgver=296
pkgbuild=1
arch=("auto")

shortdesc=("Terminal emulator for X")
longdesc=("The xterm program is a terminal emulator for the X Window System. It provides DEC VT102/VT220 (VTxxx) and Tektronix 4014 compatible terminals for programs that cannot use the window system directly.")

tags=("x11-apps xserver")

source=("ftp://invisible-island.net/xterm/xterm-${pkgver}.tgz")
build_deps=("fontconfig ncurses utempter luit libICE libSM libX11 \
    libXau libXaw libxcb libXdmcp libXext libXft libXmu libXpm    \
    libXrender libXt util-linux-ng")

BUILD_SYSTEM='autotools'
BUILD_KEYS="--prefix=/usr \
  --sysconfdir=/etc \
  --localstatedir=/var \
  --infodir=/usr/info \
  --mandir=/usr/man \
  --with-app-defaults=/etc/X11/app-defaults \
  --enable-wide-chars \
  --disable-full-tgetent \
  --disable-imake \
  --enable-88-color \
  --enable-256-color \
  --enable-ansi-color \
  --enable-broken-osc \
  --enable-broken-st \
  --enable-load-vt-fonts \
  --enable-i18n \
  --enable-doublechars \
  --enable-warnings \
  --enable-tcap-query \
  --enable-dabbrev \
  --enable-logging \
  --enable-freetype \
  --enable-luit \
  --enable-mini-luit \
  --enable-narrowproto \
  --enable-exec-xterm \
  --with-tty-group=tty \
  --with-utempter \
  --with-x \
  --build=$ARCH-slackware-linux"

INSTALL_KEYS="DESTDIR=$pkgdir"
