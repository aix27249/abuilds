# ABUILD generated by mkpkg_generator.sh

pkgname=gexiv2
pkgver=0.3.0
pkgbuild=1
arch=("auto")

shortdesc=("GObject wrapper around exiv2")
longdesc=("GObject wrapper around exiv2")

tags=("libs dev-libs")

source=("http://yorba.org/download/gexiv2/0.3/libgexiv2-${pkgver}.tar.bz2")

SLKCFLAGS="$SLKCFLAGS LIB=lib64"
BUILD_SYSTEM="autotools"
MAKE_WORD="LIB=lib64 make -j${numjobs}"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib${LIBDIRSUFFIX} --disable-static"
INSTALL_KEYS="DESTDIR=$pkgdir LIB=lib${LIBDIRSUFFIX}"

after_build() {
	set -e
	sed -i "s:lib$:lib${LIBDIRSUFFIX}:g" ${pkgdir}/usr/lib${LIBDIRSUFFIX}/pkgconfig/gexiv2.pc
	# Remove static trash
	rm ${pkgdir}/usr/lib${LIBDIRSUFFIX}/*.a
	rm ${pkgdir}/usr/lib${LIBDIRSUFFIX}/*.la
	set +e
}
