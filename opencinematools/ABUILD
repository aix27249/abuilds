# ABUILD generated by mkpkg_generator.sh

pkgname=opencinematools
pkgver=1.1.2
pkgbuild=1
arch=("auto")

shortdesc=("Open Cinema Tools: tool for creation of digital cinema packages (DCP)")

tags=("xapps media-video")

source=("https://launchpad.net/~tim-klingt/+archive/opencinematools/+files/opencinematools_${pkgver}-2~natty.tar.gz")

build_deps="asdcplib openjpeg"

build() {
	go_src_dir
	burn_patches
	set -e
	mkdir bin
	make -C build/unix all
	
	install -d ${pkgdir}/usr/bin
	for i in mkcpl mkpkl mkmap ; do
		install -Dm755 bin/$i ${pkgdir}/usr/bin/
	done

	set +e

}
