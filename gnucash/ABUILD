# ABUILD generated by mkpkg_generator.sh

pkgname=gnucash
pkgver=2.4.7
pkgbuild=1
arch=("auto")

shortdesc=("A personal and small-business financial-accounting application")

tags=("xapps app-office")

source=("http://downloads.sourceforge.net/sourceforge/${pkgname}/${pkgname}-${pkgver}.tar.bz2")

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc --sysconfdir=/etc --libexecdir=/usr/lib${LIBDIRSUFFIX}"
INSTALL_KEYS="GCONF_DISABLE_MAKEFILE_SCHEMA_INSTALL=1 DESTDIR=$pkgdir"

after_build() {
	install -dm755 "${pkgdir}/usr/share/gconf/schemas"
	gconf-merge-schema "${pkgdir}/usr/share/gconf/schemas/${pkgname}.schemas" --domain gnucash "${pkgdir}"/etc/gconf/schemas/*.schemas
	rm -f "${pkgdir}"/etc/gconf/schemas/*.schemas
}
