pkgname=perl-Archive-Zip
pkgver=1.30
pkgbuild=6
arch=('noarch')
shortdesc="perl interface to zip archives (Virtual package)"

tags="virtual dev-libs"

build_deps="bash"

adddep="perl-archive-zip"

build(){
 echo "End build"
}