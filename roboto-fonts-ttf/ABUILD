# ABUILD generated by mkpkg_generator.sh

pkgname=roboto-fonts-ttf
pkgver=20111129
pkgbuild=1
arch=("noarch")

shortdesc=("Roboto fonts, which were developed for Android 4.0")

tags=("media-fonts xserver")

source=("https://dl-ssl.google.com/android/design/Roboto_Hinted_${pkgver}.zip")

build() {
	go_src_dir
	mkdir -p ${pkgdir}/usr/share/fonts/TTF
	install -m0644 *.ttf ${pkgdir}/usr/share/fonts/TTF/
}
