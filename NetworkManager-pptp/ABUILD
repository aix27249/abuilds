
pkgname=NetworkManager-pptp
pkgver=0.9.2.0
pkgbuild=3
arch="noarch"

shortdesc=("NetworkManager PPTP plugin (Virtual package.)")

tags="virtual net-misc"

adddep="networkmanager-pptp"

build_deps="bash"

build(){
  echo "End build"
}
