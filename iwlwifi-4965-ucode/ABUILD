# ABUILD generated by mkpkg_generator.sh

pkgname=iwlwifi-4965-ucode
pkgver=228.61.2.24
pkgbuild=2
arch=("fw")

shortdesc=("iwlwifi-4965-ucode (Intel 4965 wireless microcode)")
longdesc=("The microcode provided in this package is required for the Intel PRO/Wireless 4965ABG/BG Network Connection Adapter driver for Linux (iwlwifi-4965) to operate. Visit Intel's Linux wireless website at this address: http://intellinuxwireless.org/")

tags=("net-wireless network")

source=("http://intellinuxwireless.org/iwlwifi/downloads/iwlwifi-4965-ucode-${pkgver}.tgz")

build() {
	go_src_dir
	mkdir -p ${pkgdir}/lib/firmware/
	cp iwlwifi-*.ucode ${pkgdir}/lib/firmware/
}
