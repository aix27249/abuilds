# ABUILD generated by spkg2abuild converter from gst-plugins-good-0.10.21-1.spkg

# Package metadata
pkgname=gst-plugins-bad
pkgver=0.10.22
pkgbuild=1
arch="auto"

# Package description
shortdesc=("GStreamer plugins - bad ones")
longdesc=("GStreamer plugins - bad ones")

# Source URL
source=("http://gstreamer.freedesktop.org/src/gst-plugins-bad/gst-plugins-bad-${pkgver}.tar.bz2")

# Tags
tags="libs media-libs"


# Build system
BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr \
--mandir=/usr/man \
--sysconfdir=/etc \
--localstatedir=/var \
--libdir=/usr/lib${LIBDIRSUFFIX} \
--disable-schemas-install \
--enable-experimental \
--disable-static \
--without-hal \
--with-package-name='GStreamer Bad Plugins (AgiliaLinux)' \
	--with-package-origin='http://www.agilialinux.ru/'"
BUILD_WORDS="make -j${numjobs} && sed -e 's/gst sys ext/gst/' -i Makefile"
INSTALL_KEYS="DESTDIR=${pkgdir}"
