# ABUILD generated by mkpkg_generator.sh

pkgname=glibc32
pkgver=2.12.2
pkgbuild=1
arch=('x86_64')

shortdesc=('Transitional virtual package from glibc32 to glibc-solibs32')
longdesc=('Transitional virtual package from glibc32 to glibc-solibs32')

tags=('virtual sys-libs')
adddep="glibc-solibs32"

build() {
	echo "Virtual package, deps: $adddep"
}
