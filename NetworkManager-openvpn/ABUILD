
pkgname=NetworkManager-openvpn
pkgver=0.9.2.0
pkgbuild=2
arch="noarch"

shortdesc=("NetworkManager OpenVPN plugin (Virtual package.)")

tags="virtual net-misc"

adddep="networkmanager-openvpn"

build_deps="bash"

build(){
  echo "End build"
}
