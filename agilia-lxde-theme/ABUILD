# ABUILD generated by mkpkg_generator.sh

pkgname=agilia-lxde-theme
pkgver=11.1
pkgbuild=1
arch=('noarch')

shortdesc=('Оформление рабочего стола LXDE')
longdesc=('Оформление по умолчанию для LXDE')
adddep="prudence-icon-theme prudence-theme"
tags=('lxde x11-themes')

build() {
	mkdir -p ${pkgdir}/etc/
	cp -aRd ${filedir}/skel ${pkgdir}/etc/
}
