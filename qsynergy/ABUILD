# ABUILD generated by mkpkg_generator.sh

pkgname=qsynergy
pkgver=0.9.1
pkgbuild=1
arch=("auto")

shortdesc=("Qt GUI for Synergy - network mouse and keyboard sharing")

tags=("xapps net-misc")

source=("http://sourceforge.net/projects/$pkgname/files/$pkgver/$pkgname-$pkgver.tar.gz")

build_deps="qt4"
adddep="synergy"

build() {
	go_src_dir
	burn_patches
	set -e
	qmake
	make -j${numjobs}
	install -Dm0755 qsynergy $pkgdir/usr/bin/qsynergy
	install -Dm0644 dist/debian/qsynergy.desktop $pkgdir/usr/share/applications/qsynergy.desktop
	install -Dm0644 dist/qsynergy.xpm $pkgdir/usr/share/pixmaps/qsynergy.xpm
	set +e
}
