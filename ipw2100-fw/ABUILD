# ABUILD generated by mkpkg_generator.sh

pkgname=ipw2100-fw
pkgver=1.3
pkgbuild=2
arch=("fw")

shortdesc=("ipw2100-fw (Intel ipw2100 wireless firmware)")
tags=("network net-wireless")

source=("http://bughost.org/firmware/${pkgname}-${pkgver}.tgz")

build_deps=""

build() {
	for i in *.fw ; do
		install -Dm0644 $i ${pkgdir}/lib/firmware/$i
	done
}
