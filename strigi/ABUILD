# ABUILD generated by mkpkg_generator.sh

pkgname=strigi
pkgver=0.7.8
pkgbuild=1
arch=('auto')

shortdesc=('strigi (fast and light desktop search engine)')
longdesc=('Strigi is a fast and light desktop search engine. It can handle a large range of file formats such as emails, office documents, media files, and file archives. It can index files that are embedded in other files. This means email attachments and files in zip files are searchable as if they were normal files on your harddisk. Homepage: http://strigi.sourceforge.net/')

tags=('app-misc libs')

build_deps="cmake gcc gcc-g++ automoc4 bzip2 libxml2 dbus"

source=("http://www.vandenoever.info/software/${pkgname}/${pkgname}-${pkgver}.tar.bz2")

BUILD_SYSTEM='cmake'
BUILD_KEYS="-DCMAKE_INSTALL_PREFIX=/usr -DLIB_SUFFIX=$LIBDIRSUFFIX \
	-DENABLE_DBUS:BOOL=ON \
	-DENABLE_EXPAT:BOOL=ON \
	-DENABLE_INOTIFY:BOOL=ON \
	-DENABLE_FAM=OFF \
	-DENABLE_CLUCENE=OFF \
	-DENABLE_CLUCENE_NG=OFF \
	-DENABLE_FFMPEG=OFF \
	-DENABLE_XINE=OFF"

INSTALL_KEYS="DESTDIR=${pkgdir}"

