# ABUILD generated by mkpkg_generator.sh

pkgname=libaudit
pkgver=1.7.13
pkgbuild=2
arch=("auto")

shortdesc=("The audit-libs package contains the dynamic libraries needed for applications to use the audit framework.")
longdesc=("The audit-libs package contains the dynamic libraries needed for applications to use the audit framework.")

tags=("libs sys-libs")

source=("http://ftp.de.debian.org/debian/pool/main/a/audit/audit_$pkgver.orig.tar.gz"
"http://ftp.de.debian.org/debian/pool/main/a/audit/audit_$pkgver-1.diff.gz n"
)

before_build() {
	go_src_dir
	set -e
	zcat ${srcache}/audit_${pkgver}-1.diff.gz | patch -p1 
	set +e
}

BUILD_SYSTEM="autotools"
BUILD_KEYS="--libdir=/lib${LIBDIRSUFFIX} --prefix=/"
INSTALL_KEYS="DESTDIR=$pkgdir"
after_build() {
	set -e
	mv ${pkgdir}/include ${pkgdir}/usr/include
	rm -rf ${pkgdir}/libexec
	rm -rf ${pkgdir}/share
	rm -rf ${pkgdir}/etc
	mkdir -p ${pkgdir}/usr/lib${LIBDIRSUFFIX}
	mv ${pkgdir}/lib${LIBDIRSUFFIX}/python2.6 ${pkgdir}/usr/lib${LIBDIRSUFFIX}/python2.6
	set +e
}

