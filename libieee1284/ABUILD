# ABUILD generated by mkpkg_generator.sh

pkgname=libieee1284
pkgver=0.2.11
pkgbuild=3
arch=("auto")

shortdesc=("A library to query devices connected in parallel port.")
longdesc=("This library is intended to be used by applications that need to communicate with (or at least identify) devices that are attached via a parallel port. For example, SANE uses it to identify scanners.")
tags=("sys-libs libs")
source=("http://downloads.sourceforge.net/sourceforge/$pkgname/$pkgname-$pkgver.tar.bz2")
build_deps="glibc-solibs"

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc"
INSTALL_KEYS="DESTDIR=$pkgdir"

