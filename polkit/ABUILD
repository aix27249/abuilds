# ABUILD generated by spkg2abuild converter from polkit-0.96-2.spkg

# Package metadata
pkgname=polkit
pkgver=0.101
pkgbuild=1
arch="auto"

# Package description
shortdesc=("Authorization Framework")
longdesc=("Application development toolkit for controlling system-wide privileges")

# Source URL
source=("http://hal.freedesktop.org/releases/polkit-${pkgver}.tar.gz")

# Tags
tags="libs sys-auth compat32"

# Manually-added dependencies
adddep="gobject-introspection"

# Build system
BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr \
--sbindir=/usr/sbin \
--mandir=/usr/man \
--libdir=/usr/lib${LIBDIRSUFFIX} \
--sysconfdir=/etc \
--localstatedir=/var \
--disable-gtk-doc-html \
--enable-introspection \
--disable-static \
--libexecdir=/usr/lib${LIBDIRSUFFIX}/polkit-1"


after_build() {
	echo ""
	install -m644 "${filedir}/polkit.pam" "${pkgdir}/etc/pam.d/polkit-1" || return 1
}

