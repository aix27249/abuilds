# ABUILD generated by mkpkg_generator.sh

pkgname=dvdauthor
pkgver=0.7.0
pkgbuild=1
arch=("auto")

shortdesc=("DVDauthor (DVD movie creator)")
longdesc=("DVDauthor is a program that will generate a DVD movie from a valid mpeg2 stream that should play when you put it in a DVD player. http://sourceforge.net/projects/dvdauthor/")

tags=("media-video utils")

source=("http://downloads.sourceforge.net/${pkgname}/${pkgname}-${pkgver}.tar.gz")


BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc"
INSTALL_KEYS="DESTDIR=$pkgdir"

