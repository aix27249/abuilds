# ABUILD generated by mkpkg_generator.sh

pkgname=openjpeg
pkgver=1.5.0
pkgbuild=2
arch=("auto")

shortdesc=("An open source JPEG 2000 codec")

tags=("libs media-libs")

source=("http://openjpeg.googlecode.com/files/openjpeg-${pkgver}.tar.gz")


before_build() {
	
	go_src_dir
	burn_patches
	rm -rf thirdparty
}

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc --disable-silent-rules --disable-static"
INSTALL_KEYS="DESTDIR=$pkgdir"

