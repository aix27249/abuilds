# ABUILD generated by mkpkg_generator.sh

pkgname=xinetd
pkgver=2.3.14
pkgbuild=1
arch=("auto")

shortdesc=("A secure replacement for inetd - internet superserver daemon")

tags=("sys-apps network")

source=("http://www.xinetd.org/xinetd-$pkgver.tar.gz")

config_files="etc/xinetd.conf
etc/conf.d/xinetd
etc/xinetd.d/servers
etc/xinetd.d/services"


BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc --without-libwrap"
INSTALL_KEYS="install-contrib DESTDIR=$pkgdir"


after_build() {
	install -Dm0755 ${filedir}/xinetd.rc6 ${pkgdir}/etc/init.d/xinetd
	install -Dm0644 ${filedir}/xinetd.confd ${pkgdir}/etc/conf.d/xinetd
	install -Dm0644 ${filedir}/servers ${pkgdir}/etc/xinetd.d/servers
	install -Dm0644 ${filedir}/services ${pkgdir}/etc/xinetd.d/services
}
