# ABUILD generated by mkpkg_generator.sh

pkgname=iat
pkgver=0.1.7
pkgbuild=2
arch=("auto")

shortdesc=("Detects many formats of cd-rom image files BIN,MDF,PDI,CDI,NRG,B5I and converts them to ISO-9660")
longdesc=("Detects many formats of cd-rom image files BIN,MDF,PDI,CDI,NRG,B5I and converts them to ISO-9660")

tags=("utils app-cdr")

source=("http://download.berlios.de/iat/$pkgname-$pkgver.tar.bz2")


BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --includedir=/usr/include/${pkgname}"
INSTALL_KEYS="DESTDIR=$pkgdir"

