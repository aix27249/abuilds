# ABUILD generated by spkg2abuild converter from pyOpenSSL-0.10-1.spkg

# Package metadata
pkgname=pyOpenSSL
pkgver=0.11
pkgbuild=2
arch="auto"

# Package description
shortdesc=("pyOpenSSL (Python wrapper for OpenSSL)")
longdesc=("This module is a rather thin wrapper around (a subset of) the OpenSSL library. With thin wrapper I mean that a lot of the object methods do nothing more than calling a corresponding function in the OpenSSL library")

# Source URL
source=("http://launchpad.net/pyopenssl/main/${pkgver}/+download/pyOpenSSL-${pkgver}.tar.gz")

# Tags
tags="dev-python libs"

# Manually-added dependencies
adddep=""

# Build system

# Before-build script
before_build() {
	echo ""
}

build() {
	go_src_dir
	burn_patches
	./setup.py install --prefix=/usr --root=${pkgdir}
}

after_build() {
	echo ""
}

