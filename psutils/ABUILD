# ABUILD generated by mkpkg_generator.sh

pkgname=psutils
pkgver=1.17
pkgbuild=1
arch=("auto")

shortdesc=("A set of postscript utilities")
longdesc=("A set of postscript utilities")

tags=("utils net-print")

source=("ftp://ftp.enst.fr/pub/unix/a2ps/${pkgname}-${pkgver}.tar.gz")

adddep="ghostscript"
build() {
	go_src_dir
	burn_patches
	set -e
	install -d -m755 "${pkgdir}/usr/include"
	install -d -m755 "${pkgdir}/usr/man"
	make -j1 -f Makefile.unix BINDIR="${pkgdir}/usr/bin" \
		INCLUDEDIR="${pkgdir}/usr/include/psutils" PERL=/usr/bin/perl \
		MANDIR="${pkgdir}/usr/man/man1" || exit 1
	make -j1 -f Makefile.unix BINDIR="${pkgdir}/usr/bin" \
		INCLUDEDIR="${pkgdir}/usr/include/psutils" PERL=/usr/bin/perl \
		MANDIR="${pkgdir}/usr/man/man1" install || exit 1
	set +e
}
