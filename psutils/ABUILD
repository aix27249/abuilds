# ABUILD generated by mkpkg_generator.sh

pkgname=psutils
pkgver=1.17
pkgbuild=2
arch=("auto")

shortdesc=("A set of postscript utilities")

tags=("utils net-print")

source=("http://ftp.archlinux.org/other/psutils/psutils-${pkgver}.tar.gz")

build_deps="make gcc kernel-headers grep binutils coreutils perl"

adddep="ghostscript"

build() {
  go_src_dir
  burn_patches
  install -d -m755 "${pkgdir}/usr/include"
  install -d -m755 "${pkgdir}/usr/man"
  make -j1 -f Makefile.unix BINDIR="${pkgdir}/usr/bin" \
              INCLUDEDIR="${pkgdir}/usr/include/psutils" PERL=/usr/bin/perl \
              MANDIR="${pkgdir}/usr/man/man1"
  make -j1 -f Makefile.unix BINDIR="${pkgdir}/usr/bin" \
              INCLUDEDIR="${pkgdir}/usr/include/psutils" PERL=/usr/bin/perl \
              MANDIR="${pkgdir}/usr/man/man1" install
}
