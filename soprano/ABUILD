# ABUILD generated by mkpkg_generator.sh

pkgname=soprano
pkgver=2.9.4
pkgbuild=2
arch=('auto')


shortdesc=('Soprano - the Qt/C++ RDF framework')

#shortdesc=('soprano (C++/Qt4 framework for RDF data)')
#longdesc=('Soprano (formally known as QRDF) is a library which provides a nice Qt interface to RDF storage solutions. It has a modular structure which allows to replace the actual RDF storage implementation used. Homepage: http://soprano.sourceforge.net')

build_deps="qt4 redland raptor2 libiodbc"
tags=('dev-libs libs')

source=("http://downloads.sourceforge.net/${pkgname}/${pkgname}-${pkgver}.tar.bz2")
#source=("git:git://anongit.kde.org/soprano.git")

BUILD_SYSTEM='cmake'
BUILD_KEYS="-DCMAKE_INSTALL_PREFIX=/usr \
	-DLIB_SUFFIX=${LIBDIRSUFFIX} \
	-DCMAKE_BUILD_TYPE=Release \
	-DSOPRANO_DISABLE_CLUCENE_INDEX=ON \
	-DCMAKE_SKIP_RPATH=OFF"
INSTALL_KEYS="DESTDIR=$pkgdir"

