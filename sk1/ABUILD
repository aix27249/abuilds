# ABUILD generated by mkpkg_generator.sh

pkgname=sk1
pkgver=0.9.1pre2_rev1383
pkgbuild=1
arch=('auto')

shortdesc=('sK1: vector graphics editor')
longdesc=('sK1 is an open source vector graphics editor similar to CorelDRAW, Adobe Illustrator, or Freehand. First of all sK1 is oriented for "prepress ready" PostScript & PDF output. The major sK1 features: CMYK colorspace support, CMYK support in Postscript, Cairo-based engine, Color management, Universal CDR importer (7-X3 versions), Modern Ttk based (former Tile widgets) user interface')

tags=('media-gfx xapps')

source=("http://sk1project.org/dc2.php?target=${pkgname}-${pkgver}.tar.gz")
adddep="sk1libs sk1sdk"
build() {
	go_src_dir
	burn_patches
	python ./setup.py install --prefix=/usr --optimize=2 --root=${pkgdir}
}
