# ABUILD generated by spkg2abuild converter from rfkill-0.4-1.spkg

# Package metadata
pkgname=rfkill
pkgver=0.5
pkgbuild=1
arch="auto"

# Package description
shortdesc=("rfkill switch tool")
longdesc=("rfkill is a small tool to query the state of the rfkill switches, buttons and subsystem interfaces.")

# Source URL
source=("http://www.kernel.org/pub/software/network/rfkill/rfkill-${pkgver}.tar.xz")
build_deps="gcc"

# Tags
tags="net-wireless network"

build() {
	go_src_dir
	burn_patches
	make -j${numjobs}
	make install DESTDIR=${pkgdir}
}
