# ABUILD generated by mkpkg_generator.sh

pkgname=m4
pkgver=1.4.15
pkgbuild=1
arch=("auto")

shortdesc=("m4 (an implementation of the UNIX macro processor)")
longdesc=("This is GNU m4, a program which copies its input to the output, expanding macros as it goes. m4 has built-in functions for including named files, running commands, doing integer arithmetic, manipulating text in various ways, recursion, etc... Macros can also be user- defined, and can take any number of arguments. GNU 'm4' was originally written by Rene Seindal, from Denmark.")

tags=("develop dev-util")

source=("ftp://ftp.gnu.org/gnu/m4/$pkgname-$pkgver.tar.xz")

BUILD_SYSTEM="autotools"
BUILD_KEYS="  --prefix=/usr \
  --libdir=/usr/lib${LIBDIRSUFFIX} \
  --mandir=/usr/man \
  --infodir=/usr/info \
  --docdir=/usr/doc/m4-${pkgver} \
  --build=$ARCH-slackware-linux"
INSTALL_KEYS="DESTDIR=$pkgdir"

