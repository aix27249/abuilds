# ABUILD generated by spkg2abuild converter from awesome-3.4.3-1.spkg

# Package metadata
pkgname=awesome
pkgver=3.4.11
pkgbuild=1
arch="auto"

# Package description
shortdesc=("awesome is a highly configurable, next generation framework window manager for X")
longdesc=("It is primarly targeted at power users, developers and any people dealing with every day computing tasks and who want to have fine-grained control on theirs graphical environment")

# Source URL
source=("http://awesome.naquadah.org/download/awesome-${pkgver}.tar.bz2")

# Tags
tags="x11-wm xapps"

# Manually-added dependencies
adddep=""

# Build system
BUILD_SYSTEM="cmake"
BUILD_KEYS="-DCMAKE_INSTALL_PREFIX=/usr \
	-DLIB_SUFFIX=${LIBDIRSUFFIX} \
	-DSYSCONFDIR=/etc"




after_build() {
	echo ""
	mkdir -p ${pkgdir}/etc/X11/xinit
	cp ${filedir}/xinitrc.awesome ${pkgdir}/etc/X11/xinit/
	mkdir -p ${pkgdir}/usr/share/apps/kdm/sessions
	( cd ${pkgdir}/usr/share/apps/kdm/sessions ; ln -s ../../../xsessions/awesome.desktop . )
}

