# ABUILD generated by mkpkg_generator.sh

pkgname=google-talkplugin
pkgver=5.4.2.0
pkgbuild=1
arch=("auto")
_arch="i386"
if [ "$ARCH" = "x86_64" ]; then
    _arch=$ARCH
fi

shortdesc=("Video chat browser plug-in for Google Talk")
tags=("network proprietary media-plugins")
source=("http://dl.google.com/linux/direct/${pkgname}_current_${_arch}.rpm o"
"http://www.google.com/intl/en/policies/terms/index.html o")
build_deps="rpm mkpkg"
add_dep="gtk+2 alsa-lib mesa"

build() {
    cp -R ${srcdir}/opt ${pkgdir}
    install -d "${pkgdir}/usr/lib"
    cp -R ${srcdir}/usr/lib*/* ${pkgdir}/usr/lib$LIBDIRSUFFIX
    # cleaning up cron update script
    rm -rf ${pkgdir}/opt/google/talkplugin/cron
    # install license file
    install -Dm644 "${srcache}/index.html" "${pkgdir}/usr/share/licenses/${pkgname}/license.html"
}
