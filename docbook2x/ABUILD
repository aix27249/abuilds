# ABUILD generated by mkpkg_generator.sh

pkgname=docbook2x
pkgver=0.8.8
pkgbuild=1
arch=("auto")

shortdesc=("A software package that converts DocBook documents into the traditional Unix man page format and the GNU Texinfo format.")

tags=("app-text console")

source=("http://downloads.sourceforge.net/$pkgname/docbook2X-$pkgver.tar.gz")


before_build() {
	go_src_dir
	autoreconf
}
BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc --program-transform-name 's,2x-,2,'"
INSTALL_KEYS="DESTDIR=$pkgdir"

after_build() {
	sed -i 's|http://docbook2x.sf.net/latest|/usr/share/docbook2X|g' \
		    $pkgdir/usr/bin/db2x_xsltproc
}
