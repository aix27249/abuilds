# ABUILD generated by spkg2abuild converter from cairomm-1.8.4-1.spkg

pkgname=cairomm
pkgver=1.10.0
pkgbuild=1
arch="auto"

shortdesc=("C++ bindings for the Cairo vector graphics library")
longdesc=("cairomm is a C++ wrapper for the cairo graphics library. It offers all the power of cairo with an interface familiar to C++ developers, including use of the Standard Template Library where it makes sense.")

source=("http://www.cairographics.org/releases/cairomm-${pkgver}.tar.gz")

tags="libs dev-cpp"

build_deps="make gcc gcc-g++ pkg-config doxygen libxslt binutils sed grep kernel-headers cairo libsigc++"

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr \
--libdir=/usr/lib${LIBDIRSUFFIX} \
--mandir=/usr/man \
--sysconfdir=/etc \
--docdir=/usr/doc/${pkgname}-${pkgver} \
--localstatedir=/var"



