# ABUILD generated by mkpkg_generator.sh

pkgname=bluez-hcidump
pkgver=2.0
pkgbuild=1
arch=("auto")

shortdesc=("bluez-hcidump (Linux Bluetooth(TM) analyzer)")
longdesc=("The bluez-hcidump package contains a network analyzer for Bluetooth(TM). The hcidump utility reads raw HCI data coming from and going to a Bluetooth device and prints commands, events and data. For more info, visit: http://www.bluez.org")

tags=("net-wireless network")

source=("http://www.kernel.org/pub/linux/bluetooth/${pkgname}-${pkgver}.tar.gz")

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib${LIBDIRSUFFIX}"
INSTALL_KEYS="DESTDIR=$pkgdir"


