# ABUILD generated by mkpkg_generator.sh

pkgname=slang
pkgver=2.2.4
pkgbuild=1
arch=("auto")

shortdesc=("slang (S-Lang interpreter version 2)")
longdesc=("S-Lang is an interpreted language that was designed from the start to be easily embedded into a program to provide it with a powerful extension language. S-Lang is also a programmer's library that permits a programmer to develop sophisticated platform-independent software. In addition to providing the S-Lang extension language, the library provides facilities for screen management, keymaps, and low-level terminal I/O.")

tags=("libs sys-libs")

source=("ftp://ftp.fu-berlin.de/pub/unix/misc/slang/v${pkgver%.*}/${pkgname}-${pkgver}.tar.bz2")

build_deps="glibc-solibs pcre zlib"
config_files="etc/slsh.rc"

# It fails to build with numjobs>1 :(
numjobs=1

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc --docdir=/usr/doc/${pkgname}-${pkgver}"
INSTALL_KEYS="DESTDIR=$pkgdir install-all"

