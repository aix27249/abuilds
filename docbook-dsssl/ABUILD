# ABUILD generated by mkpkg_generator.sh

pkgname=docbook-dsssl
pkgver=1.79
pkgbuild=1
arch=("noarch")

shortdesc=("DSSSL Stylesheers")

tags=("develop app-text")

source=("http://downloads.sourceforge.net/docbook/docbook-dsssl-${pkgver}.tar.bz2")
adddep="sgml-common"
build_deps="sgml-common"

build() {
	go_src_dir
	# Install:
	install -Dm0755 bin/collateindex.pl ${pkgdir}/usr/bin/collateindex.pl
	install -Dm0644 bin/collateindex.pl.1 ${pkgdir}/usr/man/man1/collateindex.pl.1
	# Eeek!  Debian trim this down but we'll leave it for the moment:
	mkdir -vpm755 ${pkgdir}/usr/share/sgml/docbook/dsssl-stylesheets-$pkgver/common
	cp -fav * ${pkgdir}/usr/share/sgml/docbook/dsssl-stylesheets-${pkgver}
	cat ${filedir}/doinst.sh | sed s/PKGVER/${pkgver}/g > ${pkgdir}/install/doinst.sh

}
