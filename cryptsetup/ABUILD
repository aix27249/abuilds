# ABUILD generated by mkpkg_generator.sh

pkgname=cryptsetup
pkgver=1.5.0
pkgbuild=1
arch=("auto")

shortdesc=("Utility for setting up encrypted filesystems")

tags=("base sys-fs")

source=("http://cryptsetup.googlecode.com/files/${pkgname}-${pkgver}.tar.bz2")

build_deps="util-linux-ng device-mapper popt libgcrypt"


BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc --enable-cryptsetup-reencrypt --sbindir=/sbin --enable-static"
INSTALL_KEYS="DESTDIR=$pkgdir"

after_build() {
	mkdir -p ${pkgdir}/lib${LIBDIRSUFFIX}
	mv ${pkgdir}/usr/lib${LIBDIRSUFFIX}/libcryptsetup.so* ${pkgdir}/lib${LIBDIRSUFFIX}/
}
