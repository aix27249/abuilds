# ABUILD generated by mkpkg_generator.sh

pkgname=mhash
pkgver=0.9.9.9
pkgbuild=1
arch=("auto")

shortdesc=("mhash (Thread-safe hash library)")
longdesc=("mhash is a thread-safe hash library, implemented in C, and provides a uniform interface to a large number of hash algorithms (MD5, SHA-1, HAVAL, RIPEMD128, RIPEMD160, TIGER, GOST). These algorithms can be used to compute checksums, message digests, and other signatures. The HMAC support implements the basics for message authentication, following RFC 2104.")

tags=("libs app-crypt")
source=("http://downloads.sourceforge.net/sourceforge/mhash/${pkgname}-${pkgver}.tar.bz2")
build_deps="glibc-solibs"

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc --enable-static"
INSTALL_KEYS="DESTDIR=$pkgdir"
