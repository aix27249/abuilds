# ABUILD generated by mkpkg_generator.sh

pkgname=kdemultimedia
pkgver=4.8.4
strict_version='yes'
pkgbuild=1
arch=('auto')

shortdesc=('kdemultimedia')
longdesc=('Multimedia applications for KDE including: * noatun: a multimedia player for sound and movies * kmix: the audio mixer as a standalone program and Kicker applet * kscd: A CD player with an interface to the internet CDDB database * phonon-xine: A Phonon backend based on Xine')

tags=('kde-base kde4')

#source="svn:svn://anonsvn.kde.org/home/kde/trunk/KDE/kdemultimedia"
source=("ftp://ftp.kde.org/pub/kde/stable/${pkgver}/src/${pkgname}-${pkgver}.tar.xz")


if [ -d /usr/lib${LIBDIRSUFFIX}/qt ]; then
  QTDIR=/usr/lib${LIBDIRSUFFIX}/qt
fi


before_build() {
 echo ""
}
BUILD_SYSTEM='cmake'
BUILD_KEYS="-DCMAKE_BUILD_TYPE=Release \
    -DCMAKE_INSTALL_PREFIX=/usr \
    -DMAN_INSTALL_DIR=/usr/man \
    -DSYSCONF_INSTALL_DIR=/etc \
    -DLIB_SUFFIX=${LIBDIRSUFFIX} \
    -DCMAKE_SKIP_RPATH=ON"
INSTALL_KEYS="DESTDIR=$pkgdir"

after_build() {
 echo ""
}

