# ABUILD generated by spkg2abuild converter from uget-1.5.0.2-1.spkg

pkgname=uget
pkgver=1.10.4
pkgbuild=1
arch="auto"

shortdesc=("Uget is a Free and Open Source download manager written in GTK+")
longdesc=("it has many of features like easy-to-use , cross-platform (Windows & GNU/Linux) , support pause and resume , classify download , every category has an independent configuration , and more.")

source=("http://downloads.sourceforge.net/project/urlget/uget%20%28stable%29/$pkgver/uget-$pkgver.tar.gz")

tags="app-misc xapps"

build_deps="make coreutils gettext pkg-config gtk+3 libnotify gstreamer curl intltool grep kernel-headers glibc-solibs aria2 xineramaproto inputproto randrproto compositeproto"

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr \
--bindir=/usr/bin \
--sbindir=/usr/sbin \
--libexecdir=/usr/libexec \
--includedir=/usr/include \
--mandir=/usr/man \
--infodir=/usr/info \
--libdir=/usr/lib${LIBDIRSUFFIX} \
--sysconfdir=/etc \
--localstatedir=/var"

