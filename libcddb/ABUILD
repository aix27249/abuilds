# ABUILD generated by mkpkg_generator.sh

pkgname=libcddb
pkgver=1.3.2
pkgbuild=3
arch=("auto")

shortdesc=("libcddb (An online CD database library)")
longdesc=("Libcddb is a library that implements the different protocols (CDDBP, HTTP, SMTP) to access data on a CDDB server (http://freedb.org). Visit the project at: http://libcddb.sourceforge.net/")

tags=("libs media-libs")

source=("http://downloads.sourceforge.net/sourceforge/${pkgname}/${pkgname}-${pkgver}.tar.bz2")
build_deps="glibc"


BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc"
INSTALL_KEYS="DESTDIR=$pkgdir"

