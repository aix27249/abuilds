# ABUILD generated by mkpkg_generator.sh

pkgname=perl-uri
pkgver=1.60
pkgbuild=2
arch=("noarch")

shortdesc=("Uniform Resource Identifiers (absolute and relative) for Perl")
tags=("dev-perl libs")
source=("http://search.cpan.org/CPAN/authors/id/G/GA/GAAS/URI-${pkgver}.tar.gz")
build_deps="perl"

docs="Changes"

BUILD_SYSTEM="perl"
BUILD_KEYS="Makefile.PL PREFIX=/usr INSTALLDIRS=vendor"

after_build(){
  go_src_dir
  make test
  rm -R  ${pkgdir}/usr/lib${LIBDIRSUFFIX}
}
