# ABUILD generated by mkpkg_generator.sh

pkgname=perl-uri
pkgver=1.58
pkgbuild=1
arch=("auto")

shortdesc=("Uniform Resource Identifiers (absolute and relative) for Perl")
longdesc=("Uniform Resource Identifiers (absolute and relative) for Perl")

tags=("dev-perl libs")

source=("http://search.cpan.org/CPAN/authors/id/G/GA/GAAS/URI-$pkgver.tar.gz")

build() {
	go_src_dir
	burn_patches
	set -e
	unset LANG
	perl Makefile.PL INSTALLDIRS=vendor
	make -j${numjobs}
	make install DESTDIR=${pkgdir}
	set +e
}
