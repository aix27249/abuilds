# ABUILD generated by mkpkg_generator.sh

pkgname=media-player-info
pkgver=18
pkgbuild=1
arch=("noarch")

shortdesc=("media-player-info is a repository of data files describing media player capabilities, mostly of mass-storage devices.")

tags=("libs media-libs")

source=("http://www.freedesktop.org/software/media-player-info/media-player-info-${pkgver}.tar.gz")

build_deps="make pkg-config coreutils python"

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr \
            --mandir=/usr/man \
            --sysconfdir=/etc \
            --with-udevdir=/etc/udev"

INSTALL_KEYS="DESTDIR=${pkgdir}"

