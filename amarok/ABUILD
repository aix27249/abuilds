# ABUILD generated by mkpkg_generator.sh

pkgname=amarok
pkgver=2.5.0
pkgbuild=2
arch=("auto")

shortdesc=("amarok (music player)")
longdesc=("Amarok is a music player for Linux and Unix with an intuitive interface. Amarok makes playing the music you love easier than ever before -- and looks good doing it. Amarok's home page is: http://amarok.kde.org")

tags=("kde4 media-sound")

source=("http://download.kde.org/stable/${pkgname}/${pkgver}/src/${pkgname}-${pkgver}.tar.bz2")
BUILD_SYSTEM="cmake"
BUILD_KEYS="-DCMAKE_INSTALL_PREFIX=/usr \
	-DLIB_SUFFIX=${LIBDIRSUFFIX} \
	-DCMAKE_BUILD_TYPE=Release"
INSTALL_KEYS="DESTDIR=$pkgdir"

