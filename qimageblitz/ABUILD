# ABUILD generated by mkpkg_generator.sh

pkgname=qimageblitz
pkgver=0.0.6
strict_version='yes'
pkgbuild=1
arch=('auto')

shortdesc=('QImageBlitz (Graphical effect and filter library for KDE4)')
longdesc=('Blitz is a graphical effect and filter library for KDE4.0 that contains many improvements over KDE 3.x kdefx library including bugfixes, memory and speed improvements, and MMX/SSE support.')

tags=('kde-base libs')

source=("svn:svn://anonsvn.kde.org/home/kde/tags/${pkgname}/${pkgver}")


build_deps='aaa_elflibs cxxlibs fontconfig glibc-solibs util-linux-ng libxcb libICE libSM libX11 libXau libXdmcp libXext libXrender qt4'

before_build() {
 echo ""
}

BUILD_SYSTEM='cmake'
BUILD_KEYS="-DCMAKE_BUILD_TYPE=Release \
    -DCMAKE_INSTALL_PREFIX=/usr \
    -DMAN_INSTALL_DIR=/usr/man \
    -DSYSCONF_INSTALL_DIR=/etc \
    -DLIB_SUFFIX=${LIBDIRSUFFIX} \
    -DCMAKE_SKIP_RPATH=ON"
INSTALL_KEYS="DESTDIR=$pkgdir"
after_build() {
 echo ""
}

