pkgname=libav
pkgver=0.8.4
pkgbuild=3
arch=('noarch')

shortdesc="libav is a complete, cross-platform solution to record, convert and stream audio and video (Virtual package)"

tags="virtual media-video"

build_deps="bash"

adddep="ffmpeg"

build(){
  echo "End build"
}

