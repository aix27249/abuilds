# ABUILD generated by mkpkg_generator.sh

pkgname=sdl_sound
pkgver=1.0.3
pkgbuild=2
arch=("auto")

shortdesc=("A library to decode several popular sound file formats, such as .WAV and .MP3")

tags=("media-libs libs")

source=("http://icculus.org/SDL_sound/downloads/SDL_sound-$pkgver.tar.gz")

build_deps="sdl libmikmod libvorbis flac speex" #smpeg broken

BUILD_SYSTEM="autotools"
BUILD_WORD="CFLAGS=\"$CFLAGS -I/usr/include/smpeg\" CXXFLAGS=\"$CFLAGS -I/usr/include/smpeg\" ./configure"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc"
INSTALL_KEYS="DESTDIR=$pkgdir"

