# ABUILD generated by mkpkg_generator.sh

pkgname=nodejs
pkgver=0.10.0
pkgbuild=1
arch=("auto")

shortdesc=("Evented I/O for V8 javascript")
longdesc=("Node.js is a platform built on Chrome's JavaScript runtime for easily building fast, scalable network applications")

tags=("server dev-lang")

source=("http://nodejs.org/dist/v${pkgver}/node-v${pkgver}.tar.gz")

build_deps="python openssl"


BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --shared-openssl --libdir=/usr/lib${LIBDIRSUFFIX}"
INSTALL_KEYS="DESTDIR=$pkgdir"

