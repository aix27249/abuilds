# ABUILD generated by mkpkg_generator.sh

pkgname=reiserfsprogs
pkgver=3.6.21
pkgbuild=2
arch=("auto")

shortdesc=("reiserfsprogs (Reiserfs filesystem utilities)")
longdesc=("These utilities are used for Reiserfs. Reiserfs is a file system based on balanced tree algorithms. Learn more at the home site: http://www.namesys.com Reiserfs is the work of Hans Reiser and many others. See the file /usr/doc/reiserfsprogs-3.6.21/README for full credits.")

tags=("base sys-fs")

source=("https://api.opensuse.org/public/source/filesystems/reiserfs/reiserfsprogs-3.6.21.tar.bz2")

build_deps="util-linux-ng"


BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --sbindir=/sbin --mandir=/usr/man --sysconfdir=/etc"
INSTALL_KEYS="DESTDIR=$pkgdir"

after_build() {
	set -e
	ln -s reiserfsck ${pkgdir}/sbin/fsck.reiserfs
	ln -s mkreiserfs ${pkgdir}/sbin/mkfs.reiserfs
	set +e
}
