# ABUILD generated by mkpkg_generator.sh

pkgname=cairo-dock-plugins
pkgver=3.0.2
pkgbuild=1
arch=("auto")

shortdesc=("OSX dock-like bar")
longdesc=("")

tags=("xapps app-misc")

source=("https://launchpad.net/cairo-dock-plug-ins/3.0/${pkgver}/+download/cairo-dock-plugins-${pkgver}.tar.gz")

build_deps=""
adddep="cairo-dock"

BUILD_SYSTEM="cmake"
BUILD_KEYS="-DCMAKE_INSTALL_PREFIX=/usr -DLIB_SUFFIX=$LIBDIRSUFFIX -DBUILD_TYPE=Release"
INSTALL_KEYS="DESTDIR=$pkgdir"

