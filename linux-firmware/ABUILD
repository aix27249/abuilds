# ABUILD generated by mkpkg_generator.sh

pkgname=linux-firmware
pkgver=3.0
pkgbuild=1
arch=("fw")

shortdesc=("A huge collection of linux firmwares")

tags=("sys-kernel base")

source=("git:git://git.kernel.org/pub/scm/linux/kernel/git/firmware/linux-firmware.git")
build_deps="mkpkg"
conflicts="kernel-firmware"

build() {
	set -e
	go_src_dir
	mkdir -p ${pkgdir}/lib/firmware
	cp -arv * ${pkgdir}/lib/firmware/
	set +e
}
