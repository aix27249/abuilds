# ABUILD generated by spkg2abuild converter from ModemManager-0.3-1.spkg

pkgname=ModemManager
pkgver=0.6.0.0
pkgbuild=1
arch="auto"

shortdesc=("Modem and mobile broadband service manager")
longdesc=("Modem and mobile broadband service manager. Should be used with NetworkManager.")

source=("http://ftp.acc.umu.se/pub/GNOME/sources/ModemManager/${pkgver%.*.*}/ModemManager-${pkgver}.tar.xz")

tags="network net-misc"

adddep="ppp>=2.4.5 NetworkManager>=0.9 usb-modeswitch"

build_deps="gcc make perl perl-xml-parser" 

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr \
--bindir=/usr/bin \
--sbindir=/usr/sbin \
--libexecdir=/usr/libexec \
--includedir=/usr/include \
--mandir=/usr/man \
--infodir=/usr/info \
--libdir=/usr/lib${LIBDIRSUFFIX} \
--sysconfdir=/etc \
--localstatedir=/var \
--disable-static"


