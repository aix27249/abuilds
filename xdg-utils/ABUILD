# ABUILD generated by mkpkg_generator.sh

pkgname=xdg-utils
pkgver=1.1.0_rc1
realver=`echo $pkgver | sed s/_/-/g`
pkgbuild=1
arch=('noarch')

shortdesc=('xdg-utils (command line tools that assist applications)')
longdesc=('Xdg-utils is a set of command line tools that assist applications with a variety of desktop integration tasks. About half of the tools focus on tasks commonly required during the installation of a desktop application and the other half focuses on integration with the desktop environment while the application is running. Homepage: http://portland.freedesktop.org/wiki/')

tags=('x11-base xserver')

#source=("svn:svn://agilialinux.ru/mpkg/tools/xdg-utils")
source=("http://portland.freedesktop.org/download/xdg-utils-${realver}.tar.gz")
adddep=("bash")

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib${LIBDIRSUFFIX} --mandir=/usr/man --sysconfdir=/etc --localstatedir=/var"

after_build() {
	install -m0755 ${filedir}/xdg-su ${pkgdir}/usr/bin/xdg-su
}
