# ABUILD generated by mkpkg_generator.sh

pkgname=qca-ossl
pkgver=2.0.0_beta3
_pkgver=${pkgver/_/-}
pkgbuild=3
arch=("auto")

shortdesc=("qca-ossl (OpenSSL plugin for QCA)")
longdesc=("This plugin provides features based on OpenSSL. It implements: * Hashing - SHA1, SHA0, RIPEMD160, MD2, MD4, MD5 * Hashing - SHA224, SHA256, SHA384 and SHA512 * Block Ciphers * Keyed Hash Message Authentication Code (HMAC), using SHA1, MD5, and RIPEMD160 * Public keys - RSA, DSA, Diffie-Hellman * PKCS")

tags=("app-crypt libs")

source=("http://delta.affinix.com/download/qca/2.0/plugins/${pkgname}-${_pkgver}.tar.bz2")



BUILD_SYSTEM="autotools"
BUILD_KEYS="--release --no-separate-debug-info"
INSTALL_KEYS="INSTALL_ROOT=$pkgdir"

