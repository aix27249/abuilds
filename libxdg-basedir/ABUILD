# ABUILD generated by mkpkg_generator.sh

pkgname=libxdg-basedir
pkgver=1.2.0
pkgbuild=1
arch=("auto")

shortdesc=("Implementation of the XDG Base Directory Specifications")
longdesc=("The XDG Base Directory Specification defines where should user files be looked for by defining one or more base directories relative in with they should be located. This library implements functions to list the directories according to the specification and provides a few higher-level functions.")
tags=("dev-libs libs")
#source=("http://n.ethz.ch/student/nevillm/download/${pkgname}/${pkgname}-${pkgver}.tar.gz")
#I do not trust this one. but old is dead somehow
source=("https://github.com/devnev/libxdg-basedir/archive/libxdg-basedir-1.2.0.tar.gz")
build_deps="glibc-solibs autoconf"

before_build() {
  go_src_dir
  ./autogen.sh
}

BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc"
INSTALL_KEYS="DESTDIR=$pkgdir"
