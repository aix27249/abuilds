# ABUILD generated by mkpkg_generator.sh

pkgname=perl-dbi
pkgver=1.616
pkgbuild=1
arch=("auto")

shortdesc=("Database independent interface for Perl")
longdesc=("Database independent interface for Perl")

tags=("dev-perl libs")

source=("http://www.cpan.org/authors/id/T/TI/TIMB/DBI-${pkgver}.tar.gz")

build() {
	go_src_dir
	burn_patches
	set -e
	unset LANG
	perl Makefile.PL INSTALLDIRS=vendor
	make -j${numjobs}
	make install DESTDIR=${pkgdir}
	set +e
}
