# ABUILD generated by mkpkg_generator.sh

pkgname=pitivi
pkgver=0.13.5
pkgbuild=2
arch=("auto")

shortdesc=("GNOME video editor")
longdesc=("PiTiVi  is a free, intuitive and featureful movie editor for the Linux desktop.")

tags=("media-video xapps")

source=("http://ftp.gnome.org/pub/GNOME/sources/pitivi/0.13/pitivi-${pkgver}.tar.bz2")

adddep="gstreamer gst-plugins-good  gst-python zope.interface pygtk>=2.14 pygoocanvas gnonlin gnome-icon-theme dbus-python gst-ffmpeg"
BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib${LIBDIRSUFFIX} --mandir=/usr/man --docdir=/usr/doc"
INSTALL_KEYS="DESTDIR=$pkgdir"

