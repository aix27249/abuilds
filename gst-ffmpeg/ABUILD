pkgname=gst-ffmpeg
pkgver=0.10.13
pkgbuild=2
arch=('auto')

shortdesc="This module contains a GStreamer plugin for FFmpeg (Virtual package.)"

tags="media-plugins virtual"

build_deps="bash"

adddep="gst-libav"

build(){
  echo "End build"
}
