# ABUILD generated by mkpkg_generator.sh

pkgname=libraw
pkgver=0.13.4
pkgbuild=1
arch=("auto")

shortdesc=("raw image decoder library")
longdesc=("raw image decoder library")

tags=("libs media-libs")

source=("http://www.libraw.su/data/LibRaw-${pkgver}.tar.gz")
numjobs=1
BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix /usr --libdir=/usr/lib${LIBDIRSUFFIX} --mandir=/usr/man"
INSTALL_KEYS="DESTDIR=$pkgdir"

