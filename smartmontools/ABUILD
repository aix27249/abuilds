# ABUILD generated by mkpkg_generator.sh

pkgname=smartmontools
pkgver=6.0
pkgbuild=1
arch=("auto")

shortdesc=("smartmontools (hard drive monitoring utilities)")

tags=("base sys-apps")

source=("http://downloads.sourceforge.net/sourceforge/${pkgname}/${pkgname}-${pkgver}.tar.gz")

build_deps="libcap-ng gcc-libs"

config_files="etc/smartd.conf"
BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr --libdir=/usr/lib$LIBDIRSUFFIX --mandir=/usr/man --sysconfdir=/etc --enable-drivedb --with-libcap-ng=yes"
INSTALL_KEYS="DESTDIR=$pkgdir"

after_build() {
	rm -rf ${pkgdir}/etc/rc.d
}
